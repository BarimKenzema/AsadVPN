name: asadvpn
description: AsadVPN (UNLIMITED)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.0
  device_info_plus: ^9.1.0
  url_launcher: ^6.1.14
  permission_handler: ^11.0.1
  flutter_localizations:
    sdk: flutter
  mobile_scanner: ^3.0.0
  
  # FIX IS HERE: Get the package directly from GitHub
  flutter_v2ray_client:
    git:
      url: https://github.com/amircybersec/flutter_v2ray_client.git
      ref: main # Use the main branch
